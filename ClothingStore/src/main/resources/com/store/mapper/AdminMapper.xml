<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.store.mapper.AdminMapper">
 <!-- 상품 등록 -->
 <insert id="productEnroll">
 	insert into product(id, name, category, brand, prod_date, price, delivery_price, discount_rate, introduction)
 	values (#{id},#{name},#{category},#{brand},#{prodDate},#{price},#{deliveryPrice},#{discountRate},#{introduction})
 </insert>
 
 </mapper>